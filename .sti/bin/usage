
#!/bin/bash -e
cat <<EOF
This is a STI activator-builder image:
To use it, install STI: https://github.com/openshift/source-to-image

Sample invocation:

sti build git://<source code> activator-builder <application image>

You can then run the resulting image via:
docker run <application image>
EOF
